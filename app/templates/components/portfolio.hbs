{{!-- Portofolio --}}
<div class="row py-4">
  <div class="col-{{colSize}} offset-{{offset}}">
    <div id="portfolio" class="portfolio clearfix row row-eq-height">
      {{#each @profiles as |profile|}}
        <ItemPortfolio @profile={{profile}} @fit={{@fit}}/>
      {{/each}}
    </div>
  </div>
</div>